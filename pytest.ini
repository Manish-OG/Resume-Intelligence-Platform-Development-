[pytest]
markers =
    slow: real model-loading tests (sentence-transformers) - excluded by default, run explicitly with `pytest -m slow` or `pytest -m "" ` to include everything.
addopts = -m "not slow"
